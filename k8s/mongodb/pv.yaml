apiVersion: v1
kind: PersistentVolume

metadata:
  name: mongo-pv
  namespace: food-app
  labels:
    type: quickbite-pv-cloud

spec:
  storageClassName: standard

  capacity:
    storage: 5Gi

  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/mongodb
